import { SkracivanjePipe } from './skracivanje.pipe';

describe('SkracivanjePipe', () => {
  it('create an instance', () => {
    const pipe = new SkracivanjePipe();
    expect(pipe).toBeTruthy();
  });
});
